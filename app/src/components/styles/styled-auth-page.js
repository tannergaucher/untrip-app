import styled from "styled-components"

const StyledAuthPage = styled.div`
  padding: 1rem;
  display: flex;

  .welcome-message {
    flex: 2;
  }

  .auth {
    flex: 1;
  }
`

export default StyledAuthPage
